module.exports = function() {
    this.Given(/^I am viewing a test result in test portal$/, function (callback) {
    });

    this.When(/^I click "([^"]*)"$/, function (arg1, callback) {
    });

    this.Then(/^it updates tagged JIRA issues with test result statuses$/, function (callback) {
    });

}