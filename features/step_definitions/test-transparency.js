module.exports = function() {
    function manualStep() {}
    this.Given(/^I am viewing a test result in test portal$/, manualStep);
    this.When(/^I click "([^"]*)"$/, manualStep);
    this.Then(/^it updates tagged JIRA issues with test result statuses$/, manualStep);
    this.Given(/^I have marked a Scenario with the @Manual tag$/, manualStep);
    this.Given(/^I have run bin\/run\-cukes against some @Manual and some automated tests$/, manualStep);
    this.Given(/^the CI Server is running$/, manualStep);
    this.When(/^I view the result in the CI Server$/, manualStep);
    this.Then(/^I can see the automated test results in a hierarchical view$/,manualStep);
    this.Then(/^the manual steps are editable and in a "([^"]*)" state$/, manualStep);
    this.Given(/^I have changed manual test results$/, manualStep);
    this.Then(/^I am notified that the save has succeeded$/, manualStep);
    this.Then(/^if I navigate to the Test Result Explorer$/, manualStep);
    this.Then(/^click back into the previously saved test result$/, manualStep);
    this.Then(/^I see it has retained my changes$/, manualStep);
    this.Given(/^I am viewing a Test Result or the Test Result Explorer$/, manualStep);
    this.When(/^I type in "([^"]*)"$/, manualStep);
    this.Then(/^the view updates to only show features \/ scenarios tagged with either @tag(\d+) or @tag(\d+)$/, manualStep);
    this.Then(/^I also see items that do not have @tag(\d+)$/, manualStep);
    this.Then(/^all the charts and summaries and statuses update to reflect the filtered view of the test$/,manualStep);
    this.When(/^I change the filter to "([^"]*)"$/, manualStep);
    this.Then(/^I only see the manual tests$/, manualStep);
    this.Then(/^I only see the automated tests$/, manualStep);
    this.Then(/^I can see both automated and manual tests$/, manualStep);
    this.Given(/^I have some tests that are not associated with a JIRA ticket$/, manualStep);
    this.When(/^I check "([^"]*)"$/, manualStep);
    this.Then(/^I only see those features \/ scenarios that have no JIRA ticket$/, manualStep);
    this.Given(/^I have tests with various statuses$/, manualStep);
    this.When(/^I check and uncheck the statuses in the upper right$/, manualStep);
    this.Then(/^the view and charts update to reflect only the subset of features \/ scenarios with the matching statuses$/, manualStep);
    this.Given(/^I have tested a JQL Query using JIRA and know it to work$/, manualStep);
    this.When(/^I paste that JQL query into the JQL text input$/, manualStep);
    this.Then(/^the view filters to show only those tickets which are associated with the JIRA tickets in the JQL result$/, manualStep);
    this.When(/^I search for tags directly by typing them or indirectly via JQL that have no test cases$/, manualStep);
    this.Then(/^I see a warning indicating how many tags were found \/ not found \/ sought$/, manualStep);
    this.Then(/^I see the names of the tags that were not found$/, manualStep);
    this.Given(/^I have some scenarios with a @SPYG\-XXX tag$/, manualStep);
    this.When(/^I click said tag name where it appears$/, manualStep);
    this.Then(/^I am taken to the JIRA ticket page for that tag$/, manualStep);
    this.Given(/^I have an up to date test result with some scenarios with @SPYG\-XXX JIRA tags$/, manualStep);
    this.Then(/^I am notified that the results have been synced$/, manualStep);
    this.Then(/^when I view the JIRA ticket\(s\) that should have been updated$/, manualStep);
    this.Then(/^I see comments in each ticket summing up the status of that ticket's test$/, manualStep);
    this.Then(/^I see summaries of scenarios, features and steps passed \/ failed \/ pending \/ not exected$/, manualStep);
    this.Then(/^I see a link that takes me to the CI Portal displaying just that story's results using pre\-filtered query$/, manualStep);
    this.Given(/^I want to publish a test result from my local computer to the CI server$/, manualStep);
    this.When(/^I do an HTTP PUT to ci\-server\-host\/test\-results\/my\-test\-result\-name\-here$/, manualStep);
    this.Then(/^I receive a (\d+) OK$/, manualStep);
    this.Then(/^I see the test result in the Test Result Explorer$/, manualStep);
    this.Then(/^I can open and view the test$/,manualStep);
}