@draft @questions @comscore
Feature: Ganglia Integration

  # Ganglia is a cluster monitoring solution that scales up to clusters of about 2000 nodes
  # It has a PHP front end and then a collector daemon as well as a metadata daemon that feed it.