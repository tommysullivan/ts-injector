#!/usr/bin/env bash
#Ensure the datetime stays in sync after refresh from a snapshot
yum install -y chrony
systemctl enable chronyd
systemctl start chronyd

# update resolv.conf
# add etc/sysconfig/network