angular.module('testPortalControllers').controller('TestResultsExplorer', function (
    $scope, $http, $location, $anchorScroll, testResultModel) {
        //TODO: Hit the server and ask for a list of test result JSONs
        //server simply goes to the configured location and sends a list of JSONs

    }
);