#!/usr/bin/env node
require('source-map-support').install();
var NodeFrameworkLoader = require('../build/lib/framework/node-framework-loader').default;
var nodeFrameworkLoader = new NodeFrameworkLoader();
nodeFrameworkLoader.loadFramework().cli.newExecutor().execute();